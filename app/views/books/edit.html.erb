<h1>Edit Book</h1>

<%= form_with model: @list do |f| %>

  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>
  <br>
  <%= f.submit 'Update Book' %>
  <br>
  <%= link_to "Show", book_path(@list.id) %>
  <%= link_to "Back", "/books"%>

<% end %>